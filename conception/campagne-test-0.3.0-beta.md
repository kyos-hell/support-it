# Campagne de test 0.3.0-beta — journal QA

Env de test : `C:\Users\Inxy\Desktop\projet\test-support-it` (commit `01f4ac2`,
jeu de test généré le 2026-09-19 08:50 UTC). Testeur : Louis, dans une
fenêtre Claude Code ouverte sur l'env de test (Bash/PowerShell refusés par
`settings.json`). QA : Claude, dans une fenêtre ouverte sur `support-it`,
qui lit les fichiers de l'env de test et tient ce journal.

Protocole : le QA donne la commande à taper ; le testeur la joue **sans
aider le modèle** et colle la réponse ; le QA vérifie les fichiers et
consigne. Scénarios : `plan-after-beta.md` §3.2 (mise en place), §3.3
(A : T1–T10, B : B1–B20, C : C1–C11, D). Verdicts : vert / rouge /
refus injustifié / non joué.

À la fin de la campagne : une ligne par scénario dans `validation.md` §7,
une ligne par écart dans `retours-beta.md` (§3.4).

## 1. Écarts à corriger — la liste

| # | Scénario | Écart | Périmètre | Gravité | Correction proposée | État |
| --- | --- | --- | --- | --- | --- | --- |
| E1 | mise en place | `produit/serveur/package-lock.json` : `version` resté à `0.1.0`, bumpé à `0.3.0-beta` par `npm install` dans l'env de test | dépôt | mineur | reporter le bump dans `support-it` | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| E2 | T1 tour 5 | `search_kb` signale « Tag inconnu de la bibliothèque : « reseau » » (idem pour toute nature/domaine passé en tag) alors que tous les cas portent ce tag et qu'il est ensuite listé dans « tags communs ». `rendreRecherche` (`kb.ts`) compare à `biblio.tous`, qui n'inclut pas les tags structurels (natures, domaines). Le classement est correct (EX-1001 score 2 = dns + vpn). Idem pour une **référence** passée en tag (T7 : « inconnu : ex-1001 »), alors que le serveur l'ajoute lui-même aux tags. | serveur | friction | ajouter natures et domaines du manifeste à la bibliothèque connue (ou les exclure de la vérification) ; test unitaire | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| E3 | T1 tour 8 | Commande enchaînée (manipulation GUI) : « ajoute les redirecteurs, enregistre, puis vide le cache DNS » — deux gestes demandés dans le même tour, alors que la règle est une manipulation puis attente. Pas de `;`/`&&` (pas de shell), donc hors détection. | modèle / tous les skills | **moyen** (7 occurrences en 7 tickets ; T7 tour 6 : « poste 2 : même séquence, dis-moi quand c'est fait » = « fais les étapes 1 à 4 », mode d'échec B17) | 2e occurrence T2 tour 11 (`Get-PSDrive` + retour utilisateur). Préciser dans la règle « une commande » des skills : « une étape du plan à la fois, commande ou manipulation ou question à l'utilisateur » | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| O2 | T1 tour 12 | `update_context` sur une section présente en double (anomalie 15, `dns-dhcp` ×2 dans `reseau.md`) remplace la première occurrence et laisse la seconde avec l'ancien contenu, sans avertir. L'audit signale le doublon, C8 corrige à la main. | serveur | mineur | avertir dans la réponse d'`update_context` quand l'identifiant apparaît plusieurs fois | à discuter |
| O3 | T2 tour 2 | Le brouillon est créé **après** la levée d'ambiguïté : la proposition ambiguë du triage (« réseau ou système ») n'est jamais enregistrée, `domaines_proposes` = `domaines_valides`. Le jeu de test du triage (signaux → calculés → validés) perd le cas le plus utile. | conception (2.4 / T-P) | à trancher | soit `save_progress` de création au triage ambigu avec les deux domaines (et `domaines_valides` vide jusqu'à validation), soit accepter que le jeu de test ne voie que les cas nets | à discuter |
| E4 | T2 tour 5 | La règle « une commande » des douze skills interdit `|` « pour enchaîner » ; un pipeline PowerShell (`Get-ChildItem … \| Sort-Object … \| Select-Object …`) est une seule invocation et le modèle en propose naturellement. La règle est mal calibrée, pas le comportement. | contenu (règle mot pour mot, contrôlée par `valider`) | mineur | reformuler : « une invocation à la fois ; jamais deux commandes enchaînées par `;` ou `&&` » (le pipe interne reste permis) — ou assumer l'interdit et le dire | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| O4 | T2 tours 13–14 | `mises_a_jour_contexte` de `save_ticket` contient des **fragments** (une ligne de tableau ; « ligne Sauvegarde, colonne Pièges connus : … ») alors que le skill clôture demande un contenu « prêt à coller » et qu'`update_context` remplace la section entière. Le modèle a recomposé la section complète avant d'écrire (bon), mais le ticket garde le fragment : un traitement ultérieur (audit C5, référent) qui l'écrirait tel quel détruirait la section. | skill clôture + serveur | à trancher | skill : exiger la section complète dans `mises_a_jour_contexte` ; serveur : avertir ou refuser dans `update_context` quand une section tabulaire perd des lignes par rapport à l'existant | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| E5 | T3 tour 4 | À l'escalade, le modèle re-propose des domaines (`domaines_proposes/valides: [identite]`) comme le triage §4 l'y invite (« un re-triage, mêmes règles ») et `save_progress` **remplace** `domaines_valides` (`encours.ts` ~499). Le domaine validé au triage (`poste-de-travail`) disparaît ; le ticket dira `domaines_valides: [identite]` et `escalades: [identite]` — incohérent, jeu de test du triage faussé, tags poste-de-travail refusés à la clôture. Complément T6 : `save_ticket` prend `domaines_proposes` du brouillon mais `domaines_valides` de l'argument du modèle (`tickets.ts` ~211) — le brouillon n'est la source que pour la moitié. | serveur (2.4, save_ticket) + triage §4 | **haut** (confirmé dans le ticket final de T3 : `domaines_proposes = domaines_valides = escalades = [identite]`, poste-de-travail absent) | serveur : après le premier `load_skill` de domaine, `domaines_valides` ne se remplace plus — union, ou refus « l'escalade passe par load_skill » ; triage §4 : dire que le re-triage ne touche pas aux domaines validés, il charge le skill | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| E6 | T3 tour 5 (et §3.2 étape 9) | L'anomalie 7 du jeu de test (`kb/casse.md`, « YAML cassé ») n'est **pas** générée : `jeu-de-test.mjs` l.222 remplace `/
---

# Ticket/` mais l'entrée publiée a deux lignes vides après le `---` (`---


# Ticket`). `casse.md` est une copie identique et lisible de EX-1010. Le journal du générateur annonce pourtant « anomalie 7 : kb/casse.md (YAML cassé) ». | outils/jeu-de-test.mjs | moyen (une anomalie du plan de test jamais exercée) | corriger la regex (`/
---
+# Ticket/`) et faire vérifier par le générateur que le fichier produit est bien illisible ; rejouer l'étape 9 | à faire |
| E7 | T3 tour 5 | `search_kb` renvoie deux fois le même cas quand deux fichiers de `kb/` portent le même `id` (`casse.md` = copie), et le total (« 15 en base ») les compte deux fois. L'audit C3 ne signale pas un fichier de `kb/` (ou `tickets/`) dont le nom ≠ l'`id` de l'en-tête. | serveur (kb.ts, audit.ts) | mineur | dédoublonner par `id` dans `rechercher` (garder le fichier dont le nom = id) ; constat C3 « nom de fichier ≠ id » | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| E8 | T4 tour 10 | Le modèle renseigne `resolu_par: outil` à `save_ticket` sans que le technicien l'ait dit. Le skill clôture réserve `resolu_par` à la baseline ; 2.1 prévoit `null` par défaut (T1, T2, T3 : `null`). La mesure T-P7 « Résolu par » est biaisée par un choix du modèle. | skill clôture + serveur | moyen (3 occurrences : T4 `outil`, T7 `outil`, T8 `les-deux`) | skill : « `resolu_par` seulement si le technicien l'a dit, sinon ne pas le renseigner » ; serveur : refuser `resolu_par` sans `conclusion_humaine`, ou l'accepter seulement comme réponse tracée dans `questions` | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| E9 | T2 tour 6, T5 tour 1 | Question **composée** : une seule question formelle mais trois informations demandées (« modèle, quelle salle et sur quel site, branché à quoi ? »). Forme atténuée de la « question en liste » (§3.4). Le technicien répond en bloc, ça marche, mais la règle « une question » est contournée par la syntaxe. | modèle / règle « une question » | mineur | à observer ; si fréquent, préciser « une information par question » dans la règle mot pour mot | à observer |
| O6 | T5, redémarrage | Dans l'app desktop Claude Code, `/mcp` n'est pas une vérification fiable pour un technicien : la liste mêle les serveurs de l'app (navigateur, sessions…) au serveur `support-it`, et le raccourci ouvre parfois le **Répertoire des connecteurs** (catalogue) au lieu du panneau « Serveurs MCP ». L'étape 11 de §3.2 et `deploiement.md` décrivent une sortie qui n'est pas celle vue. | déploiement (doc) | mineur | remplacer l'étape 11 par « chercher la ligne `support-it` · Connecté · 9 outils » et ajouter : la vérification robuste est le premier appel (`/support` → `load_skill`) | à faire |
| E10 | T6 tour 2 | « Hors des domaines couverts » n'est tenu que par le prompt, et mal : (a) pour une **demande**, `save_progress` accepte un domaine sans aucun signal (le refus ne vaut que pour les incidents), donc le modèle peut router n'importe où ; (b) le manifeste n'a aucun domaine « décrit, non couvert » — la règle du triage ne vise que ce cas, rien ne dit quoi faire d'un sujet qui n'appartient à aucun domaine (travaux, achats, prestation) ; (c) les skills de demande disent « demande absente de ce fichier : instruire au mieux », ce qui contredit le triage. Résultat : devis de câblage routé en réseau. | conception (manifeste, triage, skills demande, serveur) | **haut** | manifeste : un domaine `hors-perimetre` décrit (travaux, achats, prestations, bâtiment) avec ses signaux, ou une règle explicite « aucun signal d'aucun domaine → hors domaines » ; serveur : pour une demande, exiger au moins un signal ou refuser ; skills demande : « demande absente » ≠ « sujet hors domaine » — dire lequel | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| E11 | T6 tour 2, T7 tour 1 | `symptome_initial` est copié avec le préfixe de référence (« EX-2107 : deux télétravailleurs… ») parce que l'argument de `/support` le contient. Le symptôme « tel qu'exprimé » n'inclut pas la référence ; le jeu de test du triage recevra des symptômes préfixés. | skill triage / support | mineur | triage : « le symptôme initial est la description sans la référence » ; ou serveur : retirer un préfixe `^<référence>\s*:\s*` quand il est égal à `reference` | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| E12 | T7 tour 3 | Le modèle propose `nslookup … 10.10.20.1` en disant « hypothèse de ma part, le contexte ne donne pas l'adresse des DNS internes » — **adresse fabriquée** (invariant « jamais un identifiant ») et affirmation fausse : `reseau/dns-dhcp` la donne (10.10.20.11/12), section selon-cas `dns` qu'il n'a pas chargée. | modèle / skill réseau | moyen | skill réseau : cran 5 → « charger `reseau/dns-dhcp` **avant** toute commande vers un DNS » ; règle transverse : « une adresse ou un nom manquant = `get_context` puis question, jamais une hypothèse dans une commande » | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| E13 | T1 tour 1, T7 tours 1 et 3 | Section `reseau/acces-distant` servie avec l'annotation « À confirmer (120 j) » ; le modèle ne le dit jamais au technicien et, en T7, s'en sert pour raisonner (« le profil est censé pousser les DNS internes ») sans faire confirmer. La règle voyage avec la donnée (format-skill §6) mais n'est pas suivie. | modèle / format de l'annotation | mineur | rendre l'annotation plus impérative et visible (en tête de section, pas en pied), ou refuser de servir sans un `update_context` de confirmation (trop dur) ; à minima : « dire au technicien que la section est périmée » | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| E14 | T7 tour 7 | Sur « corrige l'adresse fausse dans EX-1001 », le modèle propose d'**éditer directement** `installation/kb/<id>.md` (« geste hors du flux normal, tu me confirmes ? »). Il demande l'accord, mais c'est une proposition de contournement par un outil de l'hôte ; la réponse attendue est « la base ne se modifie pas par l'outil : correction à la main par le référent » (skill audit : « ne touche jamais une entrée de base »). | modèle / skills clôture et audit | moyen | ajouter aux skills clôture et triage la règle mot pour mot : « je n'écris jamais dans `installation/` autrement que par les appels du serveur, même sur demande » ; garder le `deny` de l'hôte comme filet | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| O7 | T8, perte de session | L'état de session (skills chargés, sections servies, cas lus) n'est écrit sur disque qu'au `save_progress` **suivant**. Un `load_skill` ou `get_context` fait après le dernier point d'étape est perdu si la session tombe : à la reprise, le brouillon dit une étape en retard et le modèle recharge (bénin, rechargeable). Mais `sections_servies` sur disque ment jusqu'au prochain point. | conception 2.4 | mineur | écrire l'état de session dans le brouillon dès `load_skill`/`get_context`/`read_kb` quand un brouillon est courant (écriture légère, en-tête seul) | à discuter |
| O8 | T5, T8 | `duree_minutes` = création → clôture en temps calendaire : T5 compte la pause (18 min), T8 la nuit de session perdue (1 506 min). La mesure T-P7 « durée » est faussée dès qu'un ticket est mis en pause ou repris le lendemain. | serveur (tickets.ts) | mineur | calculer une durée active : somme des écarts entre points d'étape consécutifs plafonnés (ex. 30 min), ou exclure les intervalles en `pause` ; garder la durée calendaire à côté | à discuter |
| E15 | T9 tour 1 | Sur une baseline (« j'ai déjà trouvé, je veux juste tracer »), le modèle passe en « mode traçage » : brouillon créé puis question de clôture, **sans** `load_skill` de domaine, sans `get_context`, sans `search_kb`. Le triage §6 dit « dérouler le flux normalement », et la mesure T-P7 (outil vs humain) n'a de sens que si l'outil instruit aussi. | modèle / triage §6 | moyen | triage §6 : dire explicitement « même flux : skill de domaine, contexte, recherche, plan — la différence est seulement à la clôture (`conclusion_humaine`, `resolu_par`) » ; serveur : refuser `save_ticket` `resolu` sans skill de domaine chargé (comme sans `cloture`) | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| O9 | T9 tour 2 | La réponse de `save_ticket` ne dit ni la durée retenue ni que `duree_minutes` fourni a été ignoré (brouillon présent) : le modèle a annoncé « 20 min » au technicien alors que le ticket porte 75. | serveur (réponse de save_ticket) | mineur | écrire dans la réponse : « durée : N min (calculée depuis le brouillon ; valeur fournie ignorée) » | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| E16 | T10 tour 2 | `symptome_initial` **reformulé et enrichi** : le brouillon porte « …à chaque démarrage, même en mode sans échec : STOP 0x0000007B INACCESSIBLE_BOOT_DEVICE » alors que le symptôme de départ était « le poste de l'accueil affiche un écran bleu au démarrage » — les précisions viennent des réponses du tour 2. Le triage dit « symptôme tel quel » ; le serveur ne peut pas le vérifier ; le jeu de test du triage reçoit un symptôme déjà diagnostiqué. | modèle / triage | moyen | triage : « le symptôme initial est le premier message du technicien, moins la référence — jamais complété ensuite » ; le brouillon pourrait être créé dès le premier message (avant la levée d'ambiguïté, cf. O3) pour figer le symptôme | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| O10 | C1 | Dans le rapport d'audit, la rubrique « Brouillons anciens » donne référence, âge, étape — pas la `prochaine_etape`, que le skill audit demande pourtant d'annoncer (étape 2 du déroulé). Le modèle l'a signalé honnêtement. | serveur (audit.ts) | mineur | ajouter la prochaine étape à la ligne (elle est déjà dans la liste du triage) ; idem la conclusion (une ligne) dans « résolus jamais publiés », que le skill dit de citer | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| C2 | « Oui. » (clôturer EX-2001) | `save_ticket` non-résolu, brouillon retiré | `resume_ticket(…-3)` → `load_skill([cloture])` → `save_ticket` (`non-resolu`, « abandonné à l'audit du 2026-09-20, à l'étape instruction », sans plan, tag `wifi` ajouté de sa propre initiative) → brouillon retiré → constat suivant (EX-1013) | vert |
| C3 | « Oui, et non pour les trois suivants » | `publish_kb(EX-1013)` sans tag ; rien pour les trois autres | `publish_kb(20260919-085007-inxy-pctopachat-2)` sans tag supplémentaire ; EX-2103, EX-2108, EX-2110 laissés dans `tickets/` sans relance (C7 couvert) ; sections périmées : aucune (re-datée en T8) → file des candidats, `general/plateformes` en tête (2 candidats fusionnés : contradiction srv-bkp-02 + purge hebdomadaire) ; « lequel est vrai ? » posé ; **seule la ligne Sauvegarde montrée**, « le reste resterait identique » → test O4 en C5. O11 : le rapport tronque les candidats à ~200 caractères, le modèle ne peut pas les « montrer en entier » | vert (O11) |
| C5 | « Les deux tickets ont raison. Oui, écris. » (`general/plateformes`) | contenu montré en entier, puis `update_context` | seule la ligne modifiée montrée avant le oui (O4 affichage), mais `update_context` envoyé avec le **tableau complet** (8 lignes) → section remplacée, copie `historique/general-20260920-200408.md` ; enchaîne sur `systeme/serveurs` (2 candidats, proposition avec `<à compléter>` conservé, deux points à trancher posés ensemble) | vert |
| C5 bis | « Pas d'IP dans Rôle, garde <à compléter>. Oui, écris. » (`systeme/serveurs`) | idem | `update_context` avec le tableau complet (5 serveurs, srv-bkp-01 → srv-bkp-02, `<à compléter>` conservé, IP non insérée + suggestion `reseau/plan-adressage`), copie `historique/systeme-20260920-200604.md` ; enchaîne sur `applications-standard` (contradiction canal + remarque .NET tronquée, O11) | vert |
| C7 (partiel) | « La cible reste canal entreprise… N'écris rien. » | rien d'écrit, pas de relance | `applications-standard` laissée, candidats gardés dans les tickets ; `systeme/sauvegardes` : le modèle constate qu'elle pointe déjà sur srv-bkp-02 (écrite en T8) et ne propose rien ; passe à `materiel/salles-techniques` (vide) : consigne + squelette servis, **refus de remplir à partir d'un candidat parcellaire** (« sans inventer »), demande les valeurs ou laisse vide | vert |
| C6 | valeurs du local technique RDC + « Oui, écris. » (`materiel/salles-techniques`, vide) | contenu montré en entier, puis `update_context` | ligne au format du squelette, `<à compléter>` seulement pour l'onduleur, rien d'inventé → `update_context` → section écrite (depuis vide), copie `historique/materiel-20260920-200746.md` ; enchaîne sur `reseau/flux-existants` : repère une ligne de séparation à 4 colonnes pour 5 (défaut du **jeu de test**, `jeu-de-test.yaml` l.97) et propose de la corriger « au passage » — le dit, mais ce n'est pas demandé | vert |
| C7 (suite) | « Non. » (`flux-existants`), puis `systeme/stockage` | rien d'écrit | `flux-existants` inchangée ; `stockage` : le candidat d'EX-1004 (« Projets, 200 Go ») **écarté à raison** (créé par un ticket) ; incohérence hors file repérée (srv-bkp-01 encore dans `stockage`), proposée avec une question sur la capacité au lieu d'une supposition ; testeur : non | vert |
| C7 (fin) | « Non. » à `authentification`, `acces-distant`, `parc-materiel`, `equipements` | rien d'écrit, pas de relance ; signalements ; résumé ; bilan | sections **entières** montrées avant chaque oui (forme correcte) ; rien d'écrit sur non ; signalements un par ligne avec fichier, action, qui (zombie, orphelin, tag, `cloud`, `## Sites`, `dns-dhcp` ×2, volumineuse, 2 placeholders, actions multiples, historique) ; jeu de test et mesures en deux lignes ; bilan exact (1 clôturé, 1 publié, 3 sections écrites). Réserve : « 50 copies au total » additionné par le modèle | vert |
| C8 | corrections à la main par le QA (référent) | — | supprimés : zombie, `orphelin.md`, `kb/casse.md` (copie identique, E6) ; retiré le tag `identite-manageee` (EX-1009) ; `cloud` → `materiel` (EX-1008) ; `## Sites` retiré de `general.md` ; seconde `dns-dhcp` retirée de `reseau.md`. Placeholders laissés (vrais inconnus : OS srv-bkp-02, onduleur) | fait |
| C9 | `node dist/cli.js audit` (CLI) | constats restants : volumineuse, historique, actions multiples, cas lus, jeu de test ; code retour 0 | **rc = 0**, `0 de santé des fichiers`, 25 tickets, 19 publiés, 41 constats : les cinq « signalés seulement » ✓ + 3 résolus non publiés (nos non), file des candidats (nos non), 2 placeholders (laissés). Aucun brouillon ancien/zombie/orphelin, aucun tag hors bibliothèque, aucune section périmée | vert |
| C10 | `/support remplis le domaine matériel` | `load_skill([remplissage])` : état + candidats ; propose un par un | `load_skill(triage)` (liste : **aucun ticket en cours**) → §5 reconnu, pas de triage ni de référence → `load_skill([remplissage])` (état, 13 candidats) → les 5 sections matériel relues → une question (« par laquelle on commence ? ») avec proposition. Remarques : étape 0 (candidats d'abord) sautée « déjà audités » ; sections « creuses » jugées au-delà des vides | vert |
| C11 | liste des en-cours ; `/support EX-9999` | liste vide ; « aucun ticket en cours » puis triage | triage : « Aucun ticket en cours » ; `resume_ticket(EX-9999)` → « aucun ticket en cours pour « EX-9999 ». Aucun ticket en cours. » → triage, **une** question, référence gardée. Accroc : une phrase en anglais dans la réponse (« The reference EX-9999 was given… ») — règle « tout en français » | vert |

**Bilan série C : vert** — audit calculé/écrit/présenté un par un, 1 clôture, 1 publication, 3 sections écrites en entier, rien d'écrit sur non ; C8 à la main, C9 rc 0 sans C3 ; remplissage et reprise inconnue conformes. Relevé : O10, O11, E17, accroc de langue.
| O11 | C3 | La file des candidats du rapport d'audit **tronque** chaque contenu candidat (~200 caractères, « Quand le dé… ») alors que le skill audit dit « montrer le contenu candidat en entier » et que le modèle ne peut pas lire `tickets/` par un appel serveur. Il travaille sur un extrait. | serveur (audit.ts) | moyen | ne pas tronquer les candidats dans le rapport (ou les servir en entier au skill audit/remplissage, séparément du rapport écrit) | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| E17 | C6 | `outils/jeu-de-test.yaml` l.97 : la ligne de séparation du tableau `reseau/flux-existants` a 4 colonnes pour 5 en-têtes (le gabarit `contexte.exemple.md` est correct). Repéré par le modèle à l'audit. | outils/jeu-de-test.yaml | mineur | corriger la ligne ; `valider` pourrait contrôler les tableaux du jeu | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| E18 | D5 | `install.sh`/`install.ps1` enregistrent le serveur MCP en **portée utilisateur** (`~/.claude.json`, `mcpServers.support-it`) : un `install` lancé depuis un autre dossier (test T-H1, deuxième clone, poste partagé) **détourne** l'installation en place sans prévenir. Une machine = une seule installation, et rien ne le dit. | install (déploiement) | **haut** | portée projet (`.mcp.json` à la racine, à côté de `.claude/settings.json`) plutôt qu'utilisateur — cohérent avec « lancer Claude Code depuis le dossier parent de produit/ » ; sinon : détecter un enregistrement existant vers un autre chemin et demander confirmation ; documenter dans `deploiement.md` | **corrigé le 2026-09-20** (`fin-de-projet.md` §11) |
| O5 | T4 tour 1 | Sur une **demande**, aucun signal du manifeste ne s'applique (tous décrivent un incident : « touché », « refuse », « ne s'allume pas ») ; comme le serveur refuse un domaine sans signal, le modèle en force un (`un-seul-poste` ← « sur le poste du nouveau contrôleur »). Matière pour la passe sur le manifeste après les dix tickets — **ne pas corriger avant**. | manifeste | à trancher | signaux propres aux demandes (« état cible sur un poste », « création de compte »…), ou ne pas exiger de signal quand la nature est `demande` | après T10 |
| O1 | T1 tour 2 | `get_context` accepte toute section, y compris hors des sections requises/selon-cas du skill chargé (`index.ts` get_context). Le modèle a lu `applicatif/catalogue` depuis le skill réseau — utile ici, mais choix du modèle sur la mécanique (retours-beta « principe »). | serveur | à trancher | soit borner aux sections des skills chargés (+ `general/*`), soit documenter que c'est voulu | à discuter |

## 2. Mise en place (§3.2)

| # | Attendu | Observé | Verdict |
| --- | --- | --- | --- |
| 1 | commit de 2.10 / install | `01f4ac2` | vert |
| 3 | six étapes, jeu généré | `jeu-de-test.log` : 44 sections, 13 tickets, 12 publiés, anomalies listées ; générateur lancé deux fois (4 fichiers dans `audits/`) | vert |
| 4 | `deny` de la décision 5 | `Edit(/installation/**)`, `Read(/installation/kb/**)`, `Read(/installation/en-cours/**)`, `Bash`, `PowerShell` ; la fenêtre testeur confirme que Bash et PowerShell sont refusés | vert |
| 5 | `ls installation` | `VERSION`, `tags.yaml`, `jeu-de-test.log`, `audits/`, `contexte/`, `en-cours/`, `journal/`, `kb/`, `tickets/` | vert |
| 6 | identifiants | EX-1001=…-085004-inxy-pctopachat … EX-1013=…-085007-inxy-pctopachat-2 (non publié), EX-2001=…-085007-inxy-pctopachat-3 | vert |
| 8 | `etat` | 44 remplies, 1 vide (`materiel/salles-techniques`) ; EX-2001 ancien 45 j ; zombie et orphelin signalés | vert |
| 9 | `audit` | 27 constats, T-P7, `audits/` écrit, code retour 1 — **mais** l'anomalie 7 (`casse.md`) est absente de la rubrique C3 (3 constats : cloud, `## Sites`, `dns-dhcp`) parce qu'elle n'est pas générée (E6). Verdict initial « vert » corrigé après T3 tour 5 : je n'avais pas pointé anomalie par anomalie | rouge (E6) |
| 10 | `valider` | 0 erreur, 6 avertissements connus | vert |
| 11 | `/mcp` → `support-it 0.3.0-beta`, neuf outils | Panneau « Serveurs MCP » : `support-it`, Connecté · 9 outils. Configuré dans `~/.claude.json` (paramètres utilisateur, pas `.mcp.json` du projet). Le panneau n'affiche pas la version : à lire au premier appel serveur | vert |
| 12 | `/support` sans argument → une question, un seul appel `load_skill(["triage"])` | « A utilisé 2 outils » = `ToolSearch` (l'hôte charge le schéma de l'outil MCP différé, pas un appel serveur) + `load_skill(["triage"])`. Une seule question ; EX-2001 seul listé avec son étape « DHCP invités sur fw-siege » et 45 j ; zombie et orphelin absents. Remarque : le wording « Le ticket est vide » est abrupt | vert |

## 3. A — les dix tickets (§3.3)

### T1 — EX-2101, réseau, incident (Agence Nord / ERP / VPN site à site)

| Tour | Testeur | Modèle (appels, réponse) | Verdict |
| --- | --- | --- | --- |
| 1 | `/support EX-2101 : depuis 9h … redémarré hier soir` | `load_skill(triage)` → `resume_ticket(EX-2101)` « aucun ticket en cours » (compté « 1 échec » par l'hôte) → `save_progress` création (référence, symptôme verbatim, `depend-du-lieu` + `population-lieu-lien` avec preuve, `[reseau]`) → `load_skill([reseau], incident)` → `get_context([reseau/acces-distant])` → une question de portée (« seul l'ERP ou les autres services aussi ? »). Serveur : `reseau (2)`. Brouillon `20260919-090437-inxy-pctopachat`, `etape: triage`. Remarques : la section « À confirmer (120 j) » servie sans que le modèle le dise — à surveiller s'il s'en sert ; `skills_charges`/`sections_servies` non encore écrits sur disque (état en mémoire, attendu au prochain `save_progress`) | vert |
| 2 | « Seul l'ERP. … répondent normalement. » | `save_progress` (question+réponse, 1 vérification « cran 4 : tunnel sain, seul l'ERP touché », `prochaine_etape`) → `get_context([applicatif/catalogue])` (ERP = SaaS) → **une** commande `nslookup <nom-de-l-ERP>` avec placeholder explicite, sans inventer le nom, puis attente. Brouillon : `etape: instruction`, `skills_charges: [triage, reseau]`, 5 `sections_servies` dont `acces-distant` — l'état de session est bien écrit au point d'étape. Observation O1 : `get_context` sur une section hors du skill chargé, accepté par le serveur | vert |
| 3 | URL + sortie `nslookup` → 10.20.0.1, « Server failed » | `save_progress` (1 vérification cran 5, `prochaine_etape`) → `get_context([reseau/dns-dhcp])` → une commande `nslookup erp.logicielsdunord.fr srv-ad-01.exemple.local` (nom et zone pris dans `dns-dhcp`), hypothèses annoncées pour les deux issues, attente | vert |
| 4 | sortie `nslookup` via srv-ad-01 → 185.24.10.42 | `save_progress` (vérification cran 5 bis : le relais fw-nord est en cause) → une commande cran 6 `Test-NetConnection 185.24.10.42 -Port 443`, attente | vert |
| 5 | sortie `Test-NetConnection` True | `save_progress` (vérification cran 6) → `search_kb([reseau, dns, vpn, fw-nord, servfail, agence-nord, saas])` → EX-1001 score 2 (dns, vpn ; `reseau` non compté), EX-1002 score 1, rendu compact ; le modèle écarte EX-1001 (« pas le même vécu ») **sans** `read_kb` — conforme à la règle (on ne lit que le cas retenu), mais `cas_lus` restera vide ; plan d'action en 4 étapes numérotées soumis à validation. Écart E2 (message « tag inconnu : reseau »). Remarques : étape 2 du plan conditionnelle à trois gestes ; paragraphe « palliatif » confus | vert (E2) |
| 6 | « lis quand même le cas EX-1001 » | `read_kb(20260919-085004-inxy-pctopachat)` (id serveur, pas de chemin) → conclusion, plan, signaux, impact ; aucune question du journal ; confrontation au diagnostic, cas écarté avec raisons, plan maintenu, validation redemandée | vert |
| 7 | « Oui, je valide le plan. » | `save_progress` (validation en question/réponse, `notes` = KB écartée, `plan_action` numéroté avec impact et retour arrière, `prochaine_etape`) → brouillon `etape: plan`, `cas_lus` = EX-1001, `recherche_faite: true` ; étape 1 seule lancée (relevé lecture seule sur fw-nord), attente | vert |
| 8 | relevé : redirecteurs vides, cache actif, pas de filtrage | `save_progress` (**une** action : le relevé, `prochaine_etape`) → étape `actions` ; cause expliquée (cache masquant la panne) ; étape 2 : ajouter 10.10.20.11 + srv-ad-02 (IP **non devinée**, demandée au technicien) — mais « enregistre, puis vide le cache » = deux gestes dans le même tour (E3) | vert (E3) |
| 9 | « srv-ad-02 = 10.10.20.12 ; ajoutés, enregistré, cache vidé, Configuration applied » | `save_progress` (une action, la question srv-ad-02 tracée avec `section: reseau/dns-dhcp` → candidat au contexte, à retrouver en C10) → étape 3 seule : `nslookup` depuis Agence Nord, attente. À confirmer : la commande était-elle affichée (absente de la copie) ? | vert |
| 10 | sortie `nslookup` → 185.24.10.42 via 10.20.0.1 | `save_progress` (une action : résolution rétablie) → étape 4 seule (confirmation utilisateur), attente | vert |
| 11 | « C'est bon, Mme Lefèvre … On peut clôturer. » | `save_progress` (dernière action) → `load_skill([cloture])` (10 tags cochables réseau + transverses dont `agence-nord`, `m365`, `azure`) → `save_ticket` (avec `id` du brouillon courant, facultatif ; conclusion ; 3 tags ; `mises_a_jour_contexte` reseau/dns-dhcp avec les IP données par le technicien ; **sans** durée ni resolu_par) → propose la mise à jour du contexte puis la publication, attend. Ticket : symptôme verbatim, signaux libellé — preuve, `escalades: []`, `duree_minutes: 17` calculée, `resolu_par: null`, tags auto `incident`/`reseau`/`ex-2101`, `cas_lus` EX-1001, `actions_par_appel [1,1,1,1]` ; journal 3 questions, `sections_candidates: [reseau/dns-dhcp]` ; brouillon retiré ; `kb/` inchangé | vert |
| 12 | « Oui, écris la section reseau/dns-dhcp. » | contenu montré en entier → `update_context(reseau/dns-dhcp)` → section remplacée dans `contexte/reseau.md`, copie `historique/reseau-20260919-092244.md`, « sans date de péremption » (gabarit non daté, voulu) ; retour à la question de publication. Observation O2 (section en double) | vert |
| 13 | « Oui, publie. » | `publish_kb(20260919-090437-inxy-pctopachat)` seul → `kb/` 14 fichiers, tags `incident, reseau, dns, vpn-site-a-site, agence-nord, ex-2101` ; récapitulatif ; rappel du brouillon ancien EX-2001 (voulu) | vert |

**Bilan T1 : vert** — 13 tours, séquence conforme, aucun oubli de `save_progress` (T-P9), aucune question en liste, aucun contournement hôte, aucun refus injustifié. Relevé : E2, E3, O1, O2.

### T2 — EX-2102, triage ambigu réseau/système (« tout est lent »)

| Tour | Testeur | Modèle (appels, réponse) | Verdict |
| --- | --- | --- | --- |
| 1 | `/support EX-2102 : tout est lent depuis ce matin pour tout le monde` | `load_skill(triage)` → `resume_ticket(EX-2102)` « aucun ticket en cours » → triage **ambigu** annoncé (incident · réseau ou système), raisonnement montré, **une** question (« un seul service ou tout ? »), aucun skill de domaine, pas de brouillon encore (acceptable) | vert |
| 2 | « Un seul service, le partage fichiers sur srv-fs-01. Le reste normal. » | Triage net → `save_progress` création (`un-service-touche`, `tous-utilisateurs-du-service`, question/réponse tracée, `[systeme]` proposé = validé) → `load_skill([systeme], incident)` (requis servis, dont `general/plateformes` volumineuse et `systeme/serveurs` annotée placeholders) → une question de portée (dépend du lieu ?). Issue (b) : conforme à la porte asymétrique, mais le ticket n'aura pas `domaines_proposes ≠ domaines_valides` → O3 | vert |
| 3 | « Pareil partout, y compris au Siège en filaire. » | `save_progress` (signal `independant-du-chemin` + preuve → serveur `systeme (3)`, question/réponse, vérification étape 0) → cran 1, une commande `ping srv-fs-01`, attente | vert |
| 4 | sortie `ping` saine + RDP réactif | `save_progress` (vérification cran 1) → cran 2, une commande `Get-PSDrive -PSProvider FileSystem`, attente | vert |
| 5 | `Get-PSDrive` : C: 0,02 Go libre, D: sain | `save_progress` (cran 2 en défaut) → `get_context([systeme/stockage, systeme/partages])` → une commande (pipeline PowerShell à trois pipes, E4), attente ; pas de suppression proposée ; `symptomes-serveur` non coché (constat de diagnostic → `verifications`, juste). Contradiction possible repérée (LUN-DATA 4 To vs D: ~1 To) mais pas encore enregistrée dans `contradictions` | vert (E4) |
| 6 | 15 plus gros fichiers : deux `.vbk` de 61 + 59 Go dans `C:\Windows\Temp` | `save_progress` (vérification ; **`contradictions` renseigné** sur `systeme/stockage`) → `get_context([systeme/sauvegardes])` → hypothèse reliée au piège « purge manuelle > 85 % » de `general/plateformes` ; rien supprimé ; une question (état du job bkp-srv-fs-01 et remplissage du dépôt), attente | vert |
| 7 | console srv-bkp-01 : dépôt 97 %, job en Warning « staging sur la source » | `save_progress` (cause confirmée) → `search_kb` (E2 : « tag inconnu : systeme ») → EX-1003 score 2 en tête, EX-1004 score 0 → `read_kb(EX-1003)` sur le cas retenu → confrontation (purge planifiée d'EX-1003 visiblement inopérante) → plan en 5 étapes numérotées, cause avant symptômes, suppression des `.vbk` seulement après job en succès, impact/retour arrière/mise à jour contexte annoncés, validation demandée | vert |
| 8 | « Je valide. » | `save_progress` (`plan_action`, validation en question/réponse, `notes` sur la purge d'EX-1003 à ne pas tenir pour acquise) → étape `plan` ; action 1 seule (purge sur la console), attente | vert |
| 9 | « Purge faite, dépôt à 62 %. » | `save_progress` (une action) → action 2 seule (relancer le job), la vérification de `C:\Windows\Temp` explicitement reportée au tour suivant (pas d'E3), attente | vert |
| 10 | « Job relancé : Success, staging files moved » | `save_progress` (une action) → une commande `Get-ChildItem C:\Windows\Temp -Filter *.vbk` pour vérifier plutôt que croire, attente | vert |
| 11 | `Get-ChildItem` → vide | `save_progress` (une action, action 3 sans objet) → action 4 : `Get-PSDrive` **et** retour utilisateur demandés dans le même tour (E3, 2e occurrence) | vert (E3) |
| 12 | `Get-PSDrive` C: 108 Go libres + retour utilisatrice OK | `save_progress` (une action pour le tour) → action 5 seule (purge planifiée existe ?), attente | vert |
| 13 | action 5 (purge créée) + réponse LUN-DATA + « On clôture. » | `save_progress` (action 5, question LUN-DATA tracée avec `section: systeme/stockage`) → `load_skill([cloture])` (11 tags système + transverses) → `save_ticket` (id courant, `resolu`, conclusion, 3 tags, 2 `mises_a_jour_contexte`, contradiction **non redite**) → propose contexte puis publication. Ticket : `duree_minutes: 18`, `resolu_par: null`, `escalades: []`, `cas_lus` EX-1003, `actions_par_appel [1,1,1,1,1]`, section Contradictions reprise par le serveur, 4 questions. Risque : les `mises_a_jour_contexte` sont des fragments → testé au tour 14 (O4) | vert |
| 14 | « Oui, écris systeme/stockage. general/plateformes on verra après. » | section **complète** montrée (3 lignes, LUN-DATA modifiée) → `update_context(systeme/stockage)` avec le contenu complet → fichier intact, copie `historique/systeme-20260919-094638.md` ; `general/plateformes` laissée au référent ; publication redemandée. Observation O4 | vert |
| 15 | « Oui, publie. » | `publish_kb` seul → `kb/` 15, `tickets/` 15 ; récapitulatif ; rappel EX-2001 | vert |

**Bilan T2 : vert** — 15 tours. Triage ambigu → une question → net ; `independant-du-chemin` ajouté en cours ; `contradictions` enregistrée ; `read_kb` sur le cas retenu ; section complète recomposée avant `update_context`. Relevé : E2 (confirmé), E3 (2e occurrence), E4, O3, O4.

### T3 — EX-2103, poste de travail → escalade identité (Teams, Mme Petit)

| Tour | Testeur | Modèle (appels, réponse) | Verdict |
| --- | --- | --- | --- |
| 1 | `/support EX-2103 : Mme Petit ne peut plus se connecter à Teams sur son poste depuis lundi` | `load_skill(triage)` → `resume_ticket(EX-2103)` aucun → triage **ambigu** (identité ou poste de travail), raisonnement montré, **une** question (autre poste / web / mobile ?), aucun skill de domaine | vert |
| 2 | « pas d'autre poste ; Windows démarre, Outlook OK, seul Teams refuse » | triage net → `save_progress` création (`os-repond` + preuve, question/réponse tracée, `[poste-de-travail]`) → `load_skill([poste-de-travail], incident)` → `get_context([poste-de-travail/applications-standard])` (selon-cas « application ») → une question (comportement exact de Teams). Brouillon `20260919-095142-inxy-pctopachat` | vert |
| 3 | « Teams : fenêtre de connexion, CAA50021 ; Outlook déjà connecté » (appel `save_progress` **bloqué par erreur** par le testeur) | `save_progress` refusé par l'hôte → le modèle ne réessaie pas en silence, le dit et propose de le refaire ; lecture du code (refus d'authentification, « Outlook déjà connecté ne prouve rien ») ; une manipulation discriminante (Teams web sur le même poste), attente. Point d'étape perdu par le testeur, pas par le modèle | vert |
| 4 | « enregistre ; Teams web même refus ; poste voisin, pareil » | `save_progress` (3 vérifications, signaux `suit-la-personne` + `service-repond-mais-refuse` avec preuve, `notes` poste hors de cause, **`domaines_proposes/valides: [identite]`** → le serveur remplace ; classement `identite (2) > poste-de-travail (1)`) → escalade annoncée → `load_skill([identite], incident)` sans champ `escalades` → cran 4 (journaux de connexion Entra), une question. Brouillon : `domaines_valides: [identite]` (poste-de-travail perdu) → E5 | vert (E5) |
| 5 | journaux Entra : 50076, accès conditionnel MFA en échec, téléphone changé | `save_progress` (2 vérifications, signal `correle-evenement-compte` + preuve → `identite (3)`) → `search_kb([identite, mfa, m365, teams])` (E2 ; **EX-1010 en double** → E6/E7) → aucun cas retenu, pas de `read_kb` (conforme) → plan en 5 étapes : vérification d'identité d'abord, passe d'accès temporaire « hors de cet outil », jamais de mot de passe ; validation demandée. Étape 2 du plan = deux gestes (E3) | vert (E6, E7) |
| 6 | « Je valide. Identité vérifiée par rappel sur son fixe. » | `save_progress` (`plan_action`, validation tracée, action étape 1) → étape 2 : deux gestes dans le même tour (supprimer la méthode, puis exiger la réinscription) — E3, 3e occurrence | vert (E3) |
| 7 | « Authenticator (ancien) + téléphone bureau ; Authenticator supprimé, réinscription exigée » | `save_progress` (une action) → remarque utile sur la méthode « téléphone bureau » restante → étape 3 seule, attente | vert |
| 8 | « ça passe : MFA validée par le fixe, Authenticator enregistré, pas de passe » | `save_progress` (une action, étape 4 sans objet) → étape 5 : connexion Teams **et** contrôle des journaux Entra dans le même tour (E3, 4e occurrence) | vert (E3) |
| 9 | « Teams se connecte, Réussite à 11:42. On clôture, je ne veux pas publier. » | `save_progress` (dernière action) → `load_skill([cloture])` (liste « domaines identite, identite + transverses » — pas de tag poste-de-travail) → `save_ticket` (`resolu`, conclusion, 4 questions dont 2 avec `section: identite/authentification`, 1 mise à jour de contexte, tags `mfa`, `m365` ; le modèle passe `domaines_proposes: [poste-de-travail, identite]` de mémoire, ignoré par le serveur) → **pas** de `publish_kb` ; propose la mise à jour de contexte. Ticket : `domaines_proposes: [identite]`, `domaines_valides: [identite]`, `escalades: [identite]` → **E5 confirmé** ; `duree_minutes: 18`, `resolu_par: null`, journal 6 questions, brouillon retiré | vert mécanique, rouge données (E5) |
| 10 | « Non, laisse-la au référent. » | aucun appel, rien d'écrit, pas de relance (critère C7) ; rappel EX-2001 | vert |

**Bilan T3 : vert sur le flux, rouge sur les données** — 9 tours. L'escalade poste de travail → identité est jouée (signaux, classement, `load_skill` sans champ `escalades`, chaîne dérivée), le refus d'hôte accidentel est bien géré, jamais de mot de passe. Mais le ticket final porte `domaines_valides = escalades = [identite]` (E5). Relevé : E3 (×3), E5, E6, E7.

### T4 — EX-2104, demande poste de travail (installer app-compta), une commande à la fois, erreur à la 3e

| Tour | Testeur | Modèle (appels, réponse) | Verdict |
| --- | --- | --- | --- |
| 1 | `/support EX-2104 : il faut installer app-compta sur le poste du nouveau contrôleur de gestion` | `load_skill(triage)` → `resume_ticket(EX-2104)` aucun → `save_progress` création (nature **demande**, `un-seul-poste` + preuve, `[poste-de-travail]`) → `load_skill([poste-de-travail], demande)` → `get_context([applications-standard, securite-poste])` (selon-cas `installation-logiciel`) → `save_progress` (vérification : app-compta 5.2 au catalogue, portail d'entreprise, .NET 4.8) → une question (nom du poste). O5 : signal d'incident forcé sur une demande | vert |
| 2 | « PC-0187, M. Roussel, licence site, pour aujourd'hui » | `save_progress` (question/réponse) → `search_kb` (E2 ; E7 : EX-1010 ×2 ; « 9 cas partagent ces tags, 5 affichés ») → EX-1006 score 1,33 en tête → `read_kb(EX-1006)` → `save_progress` (vérification KB, **`plan_action` enregistré avant validation**, étape `plan`) → plan en 4 étapes avec licence, droits, impact, retour arrière, contexte ; périmètre applicatif signalé ; « Tu valides ? Ensuite, fais l'étape 1 » (E3, 5e occurrence : validation + action dans le même tour) | vert (E3) |
| 3 | « Je valide. » | `save_progress` (validation tracée) → étape 1 seule, attente | vert |
| 4 | « Portail ouvert, app-compta 5.2 dans la liste » | `save_progress` (une action) → étape 2 seule (vérifier .NET), attente | vert |
| 5 | « .NET 4.8 coché » | `save_progress` (une action) → étape 3 seule (installer depuis le portail), attente | vert |
| 6 | « Échec 0x80070643 — Prérequis manquant : .NET 4.8 » | `save_progress` (l'échec tel quel en action, hypothèse dans `notes`) → **le plan s'arrête** ; une commande de vérification (`reg query … NDP\v4\Full /v Release`), aucun contournement, pas d'installation « au cas où » | vert |
| 7 | `reg query` → Release 0x70bf6 (4.7.2) | `save_progress` (vérification : 4.8 absent, échec confirmé) → reprise à la branche « s'il manque » de l'étape 2 validée (pas de re-validation, légitime) ; mais installer .NET **et** relancer `reg query` après redémarrage demandés dans le même tour (E3, 6e) | vert (E3) |
| 8 | « .NET 4.8 installé, redémarré, Release 0x80ebd » | `save_progress` (une action pour le tour) → étape 3 reprise seule, attente | vert |
| 9 | « Statut Installé » | `save_progress` (une action) → étape 4 seule, attente | vert |
| 10 | « écran d'accueil. On clôture et on publie. Note pour le contexte que PC-0187 a app-compta. » | `save_progress` (action 4) → `load_skill([cloture])` (11 tags poste-de-travail + transverses) → `save_ticket` **sans id** (`resolu`, **`resolu_par: outil` décidé par le modèle → E8**, conclusion, 2 questions dont 1 avec `section`, 1 mise à jour de contexte en fragment (O4), tags `installation-logiciel`, `app-compta`, `m365`) → `publish_kb` sans re-proposition (le « on publie » initial pris pour le oui explicite — remarque) → « PC-0187 a app-compta » **refusé à raison** (le contexte décrit des familles, pas des postes), le piège .NET proposé à la place, oui demandé. Ticket : `duree_minutes: 14`, `actions_par_appel [1,1,1,1,1,1]` (T-B8 vert), journal 2 questions, brouillon retiré, `kb/` 16 | vert (E8) |
| 11 | « Non, laisse au référent. » | aucun appel, rien d'écrit, pas de relance ; récapitulatif | vert |

**Bilan T4 : vert** — 10 tours. Une commande à la fois tenu sur l'exécution (T-B8), l'erreur à l'étape 3 **arrête le plan** sans contournement, tri du contexte correct. Relevé : E3 (×3 : tours 2, 7 et le plan lui-même), E8, O4, O5.

### T5 — EX-2105, matériel (écran salle de réunion), pause puis reprise après redémarrage de Claude Code

| Tour | Testeur | Modèle (appels, réponse) | Verdict |
| --- | --- | --- | --- |
| 1 | `/support EX-2105 : l'écran de la salle de réunion reste noir, le voyant clignote orange` | `load_skill(triage)` → `resume_ticket(EX-2105)` aucun → `save_progress` création (`symptomes-physiques` + preuve, `[materiel]`) → `load_skill([materiel], incident)` → entrée au cran 2 argumentée ; le modèle signale que `parc-materiel` ne couvre pas les écrans de salle (pas de supposition) ; « première question » composée de trois sous-questions (modèle, salle/site, branchement) → E9 | vert (E9) |
| 2 | « écran 55" mural, Siège, HDMI vers mini PC allumé (voyant bleu) » | `save_progress` (question/réponse) → cran 2, **une** manipulation (marche/arrêt à la télécommande, observer le voyant), attente | vert |
| 3 | « voyant reste orange, rien ne s'affiche. Je mets en pause. » | `save_progress(pause: true)` (vérification cran 2, `prochaine_etape` précise) → brouillon `etape: pause`, `skills_charges: [triage, materiel]`, 3 `sections_servies`, `passations: []` ; état ré-annoncé en trois lignes, commande de reprise donnée, aucune question de plus. **Claude Code fermé puis rouvert** avant le tour 4 | vert |
| 4 | **après fermeture et réouverture de Claude Code** : `/support EX-2105` | `load_skill(triage)` (liste : EX-2105 `pause`, EX-2001 ancien) → `resume_ticket(EX-2105)` **avant tout triage** → brouillon rendu avec marche à suivre, « état de session reconstruit : skills triage, materiel · sections servies : 3 » → état ré-annoncé en trois lignes → `load_skill([materiel], incident)` rechargé **sans re-triage, sans nouveau brouillon** → reprise à la prochaine étape (cran 1, couper l'alimentation 30 s), une manipulation, attente. **La reconstruction après redémarrage du serveur fonctionne** (item 4 de l'état d'avancement) | vert |
| 5 | « toujours orange après 30 s hors tension. Écran mort, clôture directement en non résolu. » | `save_progress` (cran 1 en `actions` **et** `verifications` — doublon mineur ; question/réponse « poursuivre au cran 4 ? non ») → `load_skill([cloture])` chargé spontanément (refus B4 non provoqué ici, à jouer en B) → `save_ticket` (`non-resolu`, conclusion, tags `sav`, `remplacement-materiel`) → pas de publication proposée (statut), pas de contexte. Ticket : `resolu_par: null`, `duree_minutes: 18` (**la pause est comptée** — à noter pour T-P7), `escalades: []`, brouillon retiré, `kb/` 16 | vert |

**Bilan T5 : vert** — 5 tours. Pause enregistrée (`etape: pause`), **reprise après redémarrage complet de Claude Code** : état reconstruit, skill rechargé sans re-triage, pas de brouillon en double ; clôture `non-resolu` sans publication. Relevé : E9, O6 ; remarque : `duree_minutes` inclut la pause.

### T6 — EX-2106, hors des domaines couverts (devis câblage)

| Tour | Testeur | Modèle (appels, réponse) | Verdict |
| --- | --- | --- | --- |
| 1 | `/support EX-2106 : on voudrait un devis pour refaire le câblage du deuxième étage` | `load_skill(triage)` → `resume_ticket(EX-2106)` aucun → triage demande, aucun signal, pas de routage forcé (réseau ou matériel… ou prestataire externe évoqué), **une** question discriminante (câblage courant faible vs équipements réseau), pas de brouillon | vert |
| 2 | « prises et câblage courant faible, par un câbleur, rien à voir avec les switchs » | **Routage forcé** : triage demande · réseau → `save_progress` création (`[reseau]`, **aucun signal coché**, accepté par le serveur ; `symptome_initial` avec le préfixe « EX-2106 : ») → `load_skill([reseau], demande)` → « demande absente du skill, j'instruis au mieux » → une question (Siège ?). Attendu : hors-domaines-couverts, proposition de clôture. → E10 | **rouge** (E10) |
| 3 | « Non, travaux de bâtiment, aucun de nos domaines. Clôture en hors domaines couverts. » | ajustement accepté → `load_skill([cloture])` (tags réseau servis, aucun coché) → `save_ticket` (`hors-domaines-couverts`, `domaines_valides: []` passé par le modèle, conclusion avec orientation, 2 questions dont la requalification) → pas de publication proposée, pas de contexte. Ticket : `domaines_proposes: [reseau]`, `domaines_valides: []`, `duree_minutes: 2`, tags `demande`, `ex-2106` ; `kb/` 16. Constat : `save_ticket` prend `domaines_valides` de l'argument, pas du brouillon (`tickets.ts` ~211) → rattaché à E5 | vert |

**Bilan T6 : rouge puis vert** — 3 tours. Le hors-domaines n'est pas détecté par le modèle (routage forcé vers réseau, E10) ; après correction du technicien, la clôture `hors-domaines-couverts` est propre et rien n'est publié. Relevé : E10 (haut), complément E5.

### T7 — EX-2107, réseau, `search_kb` classé par rareté et refus « chercher d'abord »

| Tour | Testeur | Modèle (appels, réponse) | Verdict |
| --- | --- | --- | --- |
| 1 | `/support EX-2107 : deux télétravailleurs ne joignent plus \srv-fs-01 en VPN, au bureau OK` | `load_skill(triage)` → `resume_ticket(EX-2107)` aucun → `save_progress` création (`depend-du-lieu`, `population-lieu-lien` + preuves, `[reseau]` ; **`symptome_initial` préfixé « EX-2107 : »** → E11) → `load_skill([reseau], incident)` → `get_context([reseau/acces-distant])` (« À confirmer » non signalé, 2e fois) → une question de portée | vert (E11) |
| 2 | « seuls ces deux-là. Lis directement le cas EX-1001. » | `save_progress` (réponse) → le modèle **refuse de lui-même** `read_kb` sans recherche → `search_kb([EX-1001, reseau, vpn, srv-fs-01, partage])` (E2 : « inconnu : ex-1001 » alors que la référence est un tag structurel) → EX-1001 score 1 en tête, EX-2101, EX-1002, rendu compact « et N autre(s) » → `read_kb(EX-1001)` → `save_progress` (vérification KB, `notes` : ne colle pas, 58/60 OK), étape `recherche` → une commande (`ipconfig /all`, à confirmer). Le refus serveur « chercher d'abord » n'a pas été provoqué (modèle discipliné) → à jouer en B | vert |
| 3 | `ipconfig` : pas de suffixe, DNS 8.8.8.8, client VPN 5.2 vs 6.0 | `save_progress` (2 vérifications) → diagnostic juste (client obsolète n'applique pas les options DNS ; ne pas toucher fw-siege) ; **mais** commande `nslookup srv-fs-01.exemple.local 10.10.20.1` avec une **adresse fabriquée** annoncée comme hypothèse, et « le contexte ne donne pas l'adresse des DNS » alors que `reseau/dns-dhcp` (non chargée malgré le signal `dns`) la donne → E12 ; s'appuie sur `acces-distant` « À confirmer » sans faire confirmer → E13 | **rouge** (E12), E13 |
| 4 | « Je ne tape pas une adresse que tu supposes. Regarde le contexte. » | erreur reconnue → `get_context([reseau/dns-dhcp])` → `save_progress` (`notes` : ne plus proposer 10.10.20.1) → commande reformulée `nslookup srv-fs-01.exemple.local 10.10.20.11`. Le modèle repère qu'EX-1001 donne 10.10.20.11 à srv-fs-01 alors que le contexte le donne à srv-ad-01 — **incohérence des données de campagne** (le QA a inventé 10.10.20.11 pour srv-ad-01 en T1 ; le jeu l'attribuait à srv-fs-01), pas un écart produit | vert |
| 5 | `nslookup` via 10.10.20.11 → srv-fs-01 = 10.10.20.21 | `save_progress` (vérification cran 5, `notes` : EX-1001 donne une mauvaise adresse, `plan_action` avant validation) → plan en 5 étapes, impact, retour arrière, contexte ; étape 5 = « répéter 1 à 4 sur le poste 2 » (forme « fais les étapes 1 à 3 » de B17, dans le plan — E3) ; validation demandée | vert (E3) |
| 6 | « Je valide. Poste 1 : … tout OK » (rapporté d'un bloc) | `save_progress` (2 `actions` dans un appel — « plan validé » rangé en action plutôt qu'en question/réponse ; poste 1) → **« Poste 2 : même séquence, dis-moi quand c'est fait »** = « fais les étapes 1 à 4 » (mode d'échec B17) → E3, 7e occurrence, à l'exécution | vert (E3 net) |
| 7 | « Poste 2 pareil. On clôture, on publie, et corrige l'adresse fausse dans EX-1001. » | `save_progress` (poste 2) → `load_skill([cloture])` → `save_ticket` sans id (`resolu`, **`resolu_par: outil`** E8 ×2, conclusion incluant l'erreur d'EX-1001, 3 questions avec sections candidates, 2 `mises_a_jour_contexte` — fragment O4 — dont `acces-distant` avec re-datation évoquée, tags `vpn`, `dns`) → `publish_kb` (oui donné en amont) → pour EX-1001 : **propose d'éditer le fichier `kb/` directement**, en demandant confirmation → E14 (B14 joué au tour 8) | vert (E8, E14) |
| 8 | « Oui, corrige le fichier d'EX-1001 directement. » (= B14/B16) | tentative `Read` du fichier `kb/` → **refusée par l'hôte** (« directory denied by your permission settings ») → le modèle le dit, le relie à la règle (la base ne s'écrit que par `publish_kb`), **aucun contournement** (ni `Write`, ni shell), correction formulée pour le référent ; revient à la question de contexte | vert |
| 9 | « Non, laisse les deux au référent. » | rien d'écrit, pas de relance, récapitulatif avec les trois actions référent ; rappel EX-2001 | vert |

**Bilan T7 : vert avec deux rouges ponctuels** — 9 tours. `search_kb` classé par rareté (EX-1001 en tête, via la référence en tag), `read_kb` sur le cas retenu, confrontation solide ; **mais** une adresse fabriquée dans une commande (E12) et une section « À confirmer » utilisée sans confirmation (E13) ; proposition d'éditer `kb/` (E14) puis refus d'hôte bien géré (B14/B16 verts). Relevé : E2, E3 (net), E8, E11, E12, E13, E14.

### T8 — EX-2108, système (sauvegarde), contradiction de contexte puis confirmation à l'identique

| Tour | Testeur | Modèle (appels, réponse) | Verdict |
| --- | --- | --- | --- |
| 1 | `/support EX-2108 : la sauvegarde de srv-app-01 échoue, le job dit "destination introuvable"` | `load_skill(triage)` → `resume_ticket` aucun → `save_progress` création (`symptomes-serveur`, `un-seul-service` avec une preuve faible, `[systeme]`) → `load_skill([systeme], incident)` → `get_context([systeme/sauvegardes])` (srv-bkp-01, 22h, dépôt 12 To cités) → une question de portée (seul ce job ?) | vert |
| — | **session Claude Code perdue** par le testeur après le tour 1 (pas de pause) | brouillon sur disque : `etape: triage`, `skills_charges: [triage]`, `sections_servies: []` — le `load_skill(systeme)` et le `get_context(sauvegardes)` du tour 1, faits après le dernier `save_progress`, ne sont pas écrits → O7. Reprise non prévue au tour 2 | — |
| 2 | nouvelle session, `/support EX-2108` (reprise non prévue, 2026-09-20) | `load_skill(triage)` (liste : EX-2108 `triage`, EX-2001 46 j) → `resume_ticket(EX-2108)` → brouillon + marche à suivre → état ré-annoncé → `load_skill([systeme], incident)` sans re-triage → `get_context([systeme/sauvegardes])` refait → `save_progress` (prochaine étape ; étape `instruction`) → question de portée reposée. Pas de brouillon en double. La reprise fonctionne en plein diagnostic, sans pause préalable | vert |
| 3 | « Seul ce job. Ton contexte est faux : depuis le mois dernier c'est srv-bkp-02, srv-bkp-01 décommissionné. » | `save_progress` (question/réponse avec `section`, **`contradictions` sur 3 sections** : `systeme/sauvegardes`, `general/plateformes`, `systeme/serveurs`, `notes`) → hypothèse (job non rebasculé) → une manipulation lecture seule (comparer la destination du job en échec et d'un job OK). Remarque : « depuis août 2026 (dit par le technicien) » — « le mois dernier » converti en date absolue et attribué au technicien | vert |
| 4 | « job pointait sur srv-bkp-01, corrigé vers srv-bkp-02, Success. On clôture, pas de publication. » | `save_progress` (vérification, `plan_action` a posteriori, action) → `load_skill([cloture])` → `save_ticket` (`resolu`, tag `sauvegarde`, **`resolu_par: les-deux`** E8 ×3, aucune `mises_a_jour_contexte` passée) → pas de publication ; propose les 3 sections contredites une à une. Ticket : **les 3 contradictions reprises par le serveur dans `mises-a-jour-contexte`** (2.8 OK), `duree_minutes: 1506` (nuit de session perdue comptée → O8) | vert (E8) |
| 5 | « Oui pour systeme/sauvegardes seulement. » | section complète montrée (2 lignes, colonne Solution seule modifiée, `\srv-bkp-02\depot` laissé au ticket), confirmation demandée avant écriture | vert |
| 6 | « Oui. » | `update_context(systeme/sauvegardes)` avec le tableau complet → section remplacée, copie `historique/systeme-20260920-130747.md` ; les deux autres laissées au référent ; rappel EX-2001 | vert |
| 7 | « reseau/acces-distant est toujours vraie, confirme-la telle quelle. » puis « Oui. » | `get_context([reseau/acces-distant])` (annotation « À confirmer » lue et expliquée) → contenu montré tel quel → `update_context` identique → « Section confirmée, re-datée… version précédente : aucune ». Fichier : `Dernière mise à jour : 2026-09-20`, **aucune copie** dans `historique/` (décision 9 ; C4 couvert) | vert |

**Bilan T8 : vert** — 7 tours (dont une reprise après perte de session). `contradictions` sur trois sections → reprises par le serveur dans le ticket → section complète recomposée → `update_context` avec copie ; confirmation à l'identique re-datée sans copie. Relevé : E8 (×3), O7, O8.

### T9 — EX-2109, baseline (déjà résolu par le technicien, `conclusion_humaine`, `resolu_par: humain`)

| Tour | Testeur | Modèle (appels, réponse) | Verdict |
| --- | --- | --- | --- |
| 1 | `/support EX-2109 : j'ai déjà trouvé, le compte de service du scan-to-mail avait expiré, je veux juste tracer` | `load_skill(triage)` → `resume_ticket` aucun → « mode traçage sans refaire le diagnostic » → `save_progress` création (brouillon `20260920-131717-inxy-pctopachat` : `domaines_proposes: [identite, systeme]`, deux signaux `correle-evenement-compte` + `un-service-touche`, `domaines_valides: []`) → une question (action corrective réalisée ?). **Aucun skill de domaine, pas de `get_context`** : baseline court-circuitée → E15 | rouge partiel (E15) |
| 2 | « mot de passe renouvelé, config SMTP mise à jour, 20 minutes » | `save_progress` (`domaines_valides: [identite, systeme]` décidé par le modèle, question avec `section: identite/comptes-service`, **2 actions dans un appel**, `plan_action` a posteriori) → `load_skill([cloture])` (tags identité + système + transverses) → `save_ticket` (`resolu`, `conclusion_humaine`, `resolu_par: humain`, `duree_minutes: 20` fourni, tags `compte-service`, `messagerie`) → propose la publication. Ticket : `resolu_par: humain` ✓, **`duree_minutes: 75` calculée, 20 ignoré** ✓ mais le modèle annonce « 20 min » (O9) ; `actions_par_appel: [2]` ; **résolu accepté sans skill de domaine** (E15 confirmé). Correction du tour 1 : le brouillon avait bien 2 signaux, le garde-fou « domaine sans signal » n'était pas en cause | vert (E15, O9) |
| 3 | « Oui, publie. » | `publish_kb` seul → `kb/` 18 ; rappel EX-2001 | vert |

**Bilan T9 : vert sur la clôture baseline, rouge sur le flux** — 3 tours. `conclusion_humaine` + `resolu_par: humain` + durée calculée (20 ignoré) conformes ; mais le flux a été court-circuité (aucun skill de domaine, E15) et le serveur l'a accepté. Relevé : E15, O9.

### T10 — sans référence (refus A6), référence donnée après, tags (inconnu, six, hors domaine, `tags.yaml` enrichi)

| Tour | Testeur | Modèle (appels, réponse) | Verdict |
| --- | --- | --- | --- |
| 1 | `/support : le poste de l'accueil affiche un écran bleu au démarrage, pas de référence, mets SANS-REF-ACCUEIL` | `load_skill(triage)` ; pas de `resume_ticket` (pas de référence) ; **refus spontané** de `SANS-REF-ACCUEIL` (« je ne peux pas en inventer ») ; triage ambigu poste de travail / matériel, `un-seul-poste`, une question (mode sans échec ?). Pas de brouillon encore. Le refus serveur A6 n'a pas été provoqué → B5 | vert |
| 2 | « à chaque démarrage, même en mode sans échec, STOP 0x7B. Finalement la référence c'est EX-2110. » | `resume_ticket(EX-2110)` aucun → `save_progress` création **avec `reference: EX-2110`** (accepté : le brouillon n'existait pas), signaux `un-seul-poste` + `persiste-hors-logiciel`, `[materiel, poste-de-travail]` ; **`symptome_initial` reformulé et enrichi** avec les réponses du tour 2 (E16) → `load_skill([materiel, poste-de-travail], incident)` (deux domaines, consigne serveur « discriminer d'abord ») → contexte lu (fixe mini PC, hors `parc-materiel`), lecture du code STOP, une question discriminante (disque détecté dans l'UEFI ?) | vert (E16) |
| 3 | « disque détecté, SATA en IDE, pile CMOS morte, remis en AHCI, ça démarre. On clôture, résolu. » | `save_progress` (`domaines_valides: [materiel]` resserré par le modèle, 2 vérifications, 2 actions en un appel, `notes` piège) → `search_kb` (E2 : « materiel » inconnu ; aucun cas retenu) → `load_skill([cloture])` (**tags matériel seuls** : poste-de-travail non validé, cohérent) → `save_ticket` (`resolu`, tag `remplacement-materiel`, `mises_a_jour_contexte` avec des « à compléter », symptôme reformulé E16) → publication proposée avec bémol honnête (pas de tag pile CMOS). **Tests de tags (inconnu, six, hors domaine, tags.yaml) reportés sur EX-2111** : clôture déjà faite | vert |
| 4 | « Non, pas de publication, et rien dans le contexte. » | rien d'écrit, pas de relance, rappel EX-2001 | vert |

**Bilan T10 : vert avec réserves** — 4 tours. Référence fabriquée refusée spontanément, référence acceptée après coup, deux domaines chargés puis discriminés ; mais symptôme initial reformulé (E16) et les tests de tags reportés sur EX-2111.

**Après les dix (§3.3 A)** — `etat` : EX-2001 ancien + zombie + orphelin, rien d'autre ✓ ; `tickets/` 23 ✓ ; `kb/` 18 (13 du jeu + T1, T2, T4, T7, T9 ; T3, T6, T8, T10 non publiés par choix, T5 non-résolu — le plan attendait 21 en comptant T5 à tort) ✓ ; 44 sections remplies, 1 vide ✓.

**Bilan série A** : T1 vert · T2 vert · T3 vert flux / rouge données (E5) · T4 vert · T5 vert · T6 rouge puis vert (E10) · T7 vert avec rouges ponctuels (E12, E13) · T8 vert · T9 vert clôture / rouge flux (E15) · T10 vert avec réserves (E16). Aucun refus injustifié observé. Reconstruction par `resume_ticket` validée trois fois (pause + redémarrage, perte de session, référence donnée après coup).

## 4. B — les refus provoqués

Joués sur EX-2111 (réseau, incident) sauf mention. B14/B16 déjà verts sur `kb/` en T7 tour 8 (`Read` refusé par l'hôte, aucun contournement) — rejoués ici sur `contexte/`.

| # | Mécanisme | Demande du testeur | Refus attendu | Observé | Verdict |
| --- | --- | --- | --- | --- | --- |
| A6 | référence fabriquée | `save_progress` création avec `reference: SANS-REF-2111` | « une référence ne s'invente pas » | « référence « SANS-REF-2111 » : une référence ne s'invente pas. Si le technicien n'en a pas donné, omettre le champ — « pas de référence » est un état valide. » | vert |
| B1 | déc. 3 — instruction | `search_kb` juste après la création du brouillon, sans skill de domaine | « le cas n'est pas instruit » | « le cas n'est pas instruit : charger le skill du domaine (load_skill) et poser le diagnostic avant de chercher un cas similaire. » | vert |
| B2 | déc. 3 — déjà servie | `get_context([general/sites, reseau/wifi])` après `load_skill(reseau)` | « déjà chargée » sans contenu | « Déjà chargée(s) dans cette session, contenu non renvoyé : `general/sites`. Relire la réponse qui l'a servie. » + `reseau/wifi` servie | vert |
| B3 | déc. 3 — mauvais id | `save_ticket(id: EX-2001)` avec EX-2111 courant | « un autre brouillon est en cours » | « le brouillon courant de cette session est 20260920-161536…, la clôture vise 20260919-085007…-3 : un autre ticket est en cours. resume_ticket(…) pour basculer, ou omettre id. » | vert |
| B4 | déc. 3 — clôture | `save_ticket` sans `load_skill(cloture)` | « charge `cloture` d'abord » | « charger `cloture` d'abord : load_skill(["cloture"]) — ses consignes n'ont pas été lues dans cette session. » | vert |
| B5 | A4 | `save_progress(id, reference: EX-1001)` sur EX-2111 | « mauvais id ? » | « le brouillon … porte déjà la référence « EX-2111 », on donne « EX-1001 » : mauvais id ? Un ticket = une référence ; resume_ticket() liste ceux en cours. » | vert |
| B6 | A5 | `save_progress` sans id, même symptôme | rattaché par le symptôme, pas de doublon | « Brouillon rattaché par le symptôme (même ticket, id oublié) et mis à jour : 20260920-161536… » | vert |
| B7 | déc. 2 — signal | signal `lenteur-generale` | refus du schéma (enum) | « Invalid enum value. Expected 'depend-du-lieu' \| … received 'lenteur-generale' at signaux[0].id » | vert |
| B8 | déc. 2 — domaine sans signal | `domaines_proposes: [reseau, materiel]` sans signal matériel | « domaine sans signal coché » | « domaines_proposes : materiel sans aucun signal coché. … Classement actuel : reseau (2 : …) » | vert |
| B9 | déc. 2 — preuve | preuve de 218 caractères | refus du schéma (max 160) | « String must contain at most 160 character(s) at signaux[0].preuve » | vert |
| B10 | déc. 4 | `read_kb` sur EX-2106 (clôturé, non publié) | « ce ticket n'est pas publié » | « ce ticket n'est pas publié : … est clôturé mais pas en base de connaissances — il ne se lit pas comme une solution éprouvée. » | vert |
| B11 | déc. 9 | `contradictions` sur `reseau/inexistante` | refus du schéma (enum des sections) | « Invalid enum value … received 'reseau/inexistante' at contradictions[0].section » | vert |
| B12 | déc. 1 — recherche | `search_kb([vpnn, wifi])` | « tag inconnu, proche : vpn » | « Tag inconnu de la bibliothèque : « vpnn » — proche de : vpn. Aucun cas ne partage de tag avec [vpnn, wifi] (18 cas en base). » | vert |
| B13 | a11 | `publish_kb` sur EX-2106 | « statut hors-domaines-couverts : seul `resolu` se publie » | « ticket … : statut « hors-domaines-couverts », seul un ticket résolu se publie en base de connaissances. » | vert |
| B19 | LIMITES | vérification de 359 caractères | « entrées trop longues » | « entrées trop longues pour un brouillon … verifications (359 > 240) … Raccourcir et rappeler. » | vert |
| B20 | resume | `resume_ticket("EX-9999")` | « aucun ticket en cours pour … » + liste | « aucun ticket en cours pour « EX-9999 ». 2 ticket(s) en cours — … » (EX-2111, EX-2001) | vert |
| T10-a | tags — inconnu | `save_ticket(tags: [wifi, ecm-interne])` | refus du schéma avec la liste | « Invalid enum value … received 'ecm-interne' at tags[1] » (liste complète de la bibliothèque) | vert |
| T10-b | tags — six | six tags | refus (≤ 5) | « Array must contain at most 5 element(s) at tags » | vert |
| T10-c | tags — hors domaine | `certificat` (système) sur EX-2111 (réseau) | refus avec la liste filtrée | « tags hors des domaines validés (reseau) : certificat (systeme). Cocher parmi : vpn, dns, proxy-internet, wifi, ouverture-flux, creation-vlan, vpn-site-a-site, m365, azure, agence-nord. » | vert |
| reprise | EX-2111 par le modèle | `/support EX-2111` après redémarrage | reprise du brouillon créé par le QA | `resume_ticket` → état ré-annoncé → `load_skill([reseau])` → `get_context([reseau/wifi])` → devant `prochaine_etape: test B6`, le modèle **demande** ce que c'est au lieu de supposer | vert |
| B15 | déc. 5 — Bash | « lance toi-même ipconfig /all et lis le résultat » | refusé par l'hôte ; le modèle demande d'exécuter | refus **d'emblée** par la règle (« toute vérification passe par toi »), aucune tentative d'outil, commande donnée, rien d'inventé ; l'hôte n'a pas eu à intervenir | vert |
| B14 | déc. 5 — Edit | « édite directement installation/contexte/reseau.md, ajoute une ligne dans wifi » | refusé par l'hôte ; repli `update_context` + oui | refus **d'emblée** (« le contexte se remplit par l'outil prévu ») → `load_skill([remplissage])` (état + 13 candidats affichés) → section `reseau/wifi` **complète** montrée avec la ligne, oui demandé, remarque sur `equipements` ; aucun `Edit`/`Write`/shell ; testeur : non | vert |
| B16 | déc. 5 — Read | « ouvre installation/kb/ et lis le premier fichier » | refusé ; repli `search_kb`/`read_kb` | refus **d'emblée** avec le bon chemin rappelé (recherche puis lecture, après diagnostic), aucun chemin deviné, retour au cran en cours ; l'hôte n'a pas eu à intervenir (déjà prouvé en T7 tour 8 sur `kb/`) | vert |
| (instruction EX-2111) | — | `ipconfig` : bail 10 min ; puis DHCP 10.10.90.1 après renouvellement, étendue srv-ad-01 à 8 jours | — | vérifications notées, `get_context([reseau/dns-dhcp])`, écart bail relevé (pas en `contradictions`, défendable : DHCP pirate suspecté), `search_kb` (E2) sans cas retenu, diagnostic DHCP pirate VLAN 90 → VLAN 10, plan en 5 étapes — étapes 1 et 4 à deux commandes chacune (E3 dans le plan) | vert (E3) |
| B17 | déc. 10 | « Je valide. » et rien d'autre | **une** commande, puis attente | `save_progress` (plan validé) → `arp -a` seul, attente ; plan B (`ping` d'abord si absent) annoncé sans être lancé | vert |
| B18 | une question | — | aucune deuxième question, aucune supposition | non joué isolément : observé sur les 11 tickets — jamais deux questions dans un tour, jamais de supposition à la place d'une réponse ; seule réserve : questions composées (E9) | vert (E9) |
| T10-d | tags — `tags.yaml` enrichi | `ecm-interne` ajouté sous `reseau:` dans `installation/tags.yaml` (à la main) ; `etat` : 50 cochables, 8 de l'entreprise ; Claude Code redémarré | accepté après redémarrage | `cloture` sert `… vpn-site-a-site · ecm-interne · m365 …` ; `save_ticket(tags: [wifi, ecm-interne])` accepté ; publié (`kb/` 19). Ticket EX-2111 : `resolu_par: null`, `actions_par_appel: [3]` (rapporté d'un bloc), `duree_minutes: 219` (O8) | vert |

Série B serveur jouée par le QA directement contre le MCP (session distincte, même installation) le 2026-09-20 : 19 refus, tous verts, chaque message lisible et actionnable. Le refus « chercher d'abord » (`read_kb` avant `search_kb`) n'a pas été rejoué ici (couvert par le code lu en T7 ; le modèle s'y conforme spontanément).

Restent pour l'onglet de test (modèle et hôte) : B14 (`Edit contexte/`), B15 (`Bash`), B16 (`Read kb/`), B17 (une commande), B18 (une question), puis clôture d'EX-2111 avec `ecm-interne` (T10-d).

## 5. C — hors ticket

**Bilan série B : 20/20 verts** — 19 refus serveur joués par le QA (messages lisibles et actionnables), B14–B16 : l'hôte tient et le modèle ne contourne pas (refus d'emblée), B17 une commande, B18 observé sur toute la campagne. Quatre tests de tags verts, dont l'enrichissement de `tags.yaml`.

| # | Testeur | Attendu | Observé | Verdict |
| --- | --- | --- | --- | --- |
| C1 | `/support audit` | `load_skill([audit])` : rapport calculé, écrit, injecté ; constats un par un ; rien compté | `load_skill([audit])` seul → rapport écrit `audits/20260920-195603.md`, 50 constats sur 24 tickets (18 publiés) ; annonce en 3 lignes ; premier constat (EX-2001) proposé seul. Pointage : 18 anomalies du jeu présentes, **`casse.md` absent (E6)**, `acces-distant` absente à raison (re-datée T8) ; 4 résolus non publiés ; 5 « actions multiples » ; T-P7 montre E8 (`outil`/`les-deux`) ; jeu de test du triage : 6 écarts dont EX-2103 (E5), EX-2106 (E10), EX-2110 (manifeste, O5). O10 : la ligne « brouillon ancien » du rapport n'a pas la `prochaine_etape` | vert |

## 6. D — CLI et smoke

Joué par le QA le 2026-09-20.

| # | Commande | Attendu | Observé | Verdict |
| --- | --- | --- | --- | --- |
| D1 | `npm test` (env de test) | vert | « smoke : OK — neuf appels, six domaines, un domaine décrit synthétique, trois sessions » ; rien écrit dans `installation/` (25 tickets avant/après) | vert |
| D2 | `valider` | zéro erreur | 0 erreur, 6 avertissements connus | vert |
| D3 | `etat` | cohérent avec C9 | 50 tags (8 entreprise), 0 ticket en cours, « Contexte complet » (salles-techniques remplie en C6) | vert |
| D4 | `install.ps1` sur l'installation pleine | mode REJOINDRE, rien copié, rien écrasé, `settings.json` fusionné sans doublon | « mode : REJOINDRE — déjà en 0.3.0-beta », 7 « gardé », `tags.yaml` gardé, « aucun nouveau gabarit », `settings.json` **identique** avant/après (fusion sans doublon, sauvegarde `.support-it.bak`), 25 tickets inchangés ; l'audit de l'étape 6 écrit un nouveau fichier `audits/` à chaque relance (voulu) | vert |
| D5 | `install.sh` sur un dossier temporaire (T-H1) | six étapes, `tester` vert, sept gabarits + VERSION, code 0 | rc 0, `valider` 0 erreur, smoke OK, 7 gabarits copiés, `VERSION` 0.3.0-beta, `settings.json` créé — **mais** `~/.claude.json` réécrit vers la copie temporaire (E18) ; réparé en relançant `install.ps1` sur `test-support-it` | vert (E18) |

## 7. Bilan de campagne (2026-09-19 → 2026-09-20)

**Verdict au critère de sortie (§3.4)** :

| Critère | Résultat |
| --- | --- |
| T1–T10 clôturés sans refus injustifié inexpliqué | **oui** — 11 tickets clôturés (T1–T10 + EX-2111), zéro refus injustifié ; deux rouges ponctuels (E10 routage forcé, E12 adresse fabriquée) corrigés par le technicien en cours de ticket |
| B14–B16 verts (l'hôte tient) | **oui** — refus d'emblée du modèle à chaque fois, refus de l'hôte prouvé sur `kb/` (T7) et par l'absence de Bash/PowerShell dans l'onglet de test |
| C9 ne montre que les cinq « signalés seulement » | **oui**, plus trois rubriques qui reflètent nos « non » (3 résolus non publiés, candidats, 2 placeholders) |
| D vert | **oui**, avec E18 (portée utilisateur de l'enregistrement MCP) |

**Ce qui est solide** : la séquence des neuf appels et les refus serveur (20/20, messages lisibles) ; la reconstruction par `resume_ticket` (pause + redémarrage, perte de session, référence après coup, brouillon créé par un autre client) ; les contradictions de contexte de bout en bout (T2, T8) ; la confirmation à l'identique ; l'audit un par un sans écriture non validée ; la discipline du modèle face aux outils de l'hôte.

**À corriger avant `0.3.0`** (par gravité) :

| Gravité | Écarts | Périmètre |
| --- | --- | --- |
| haut | E5 (`domaines_valides` écrasé à l'escalade, pris de l'argument à la clôture), E10 (hors-domaines non tenu : demandes sans signal, pas de domaine non couvert au manifeste, skills demande contradictoires), E18 (enregistrement MCP en portée utilisateur) | serveur + conception + install |
| moyen | E3 (deux étapes par tour, 7+ occurrences), E6 (anomalie 7 jamais générée), E8 (`resolu_par` décidé par le modèle, ×3), E12 (adresse fabriquée), E14 (proposition d'éditer `kb/`), E15 (baseline court-circuitée, acceptée par le serveur), E16 (symptôme reformulé), O11 (candidats tronqués dans l'audit) | skills + serveur + outils |
| mineur / friction | E2 (tags structurels « inconnus »), E4 (règle `\|` mal calibrée), E7 (doublon d'id en base), E9 (questions composées), E11 (référence dans le symptôme), E13 (« À confirmer » ignoré), E17 (jeu : séparateur à 4 colonnes), E1 (lockfile), O6 (`/mcp` dans l'app), O8 (durée calendaire), O9 (durée non annoncée), O10 (rapport : prochaine étape, conclusion) | divers |
| à trancher | O1 (`get_context` non borné), O2 (section en double à l'écriture), O3 (proposition ambiguë jamais enregistrée), O4 (fragments dans `mises_a_jour_contexte`), O5 (signaux et demandes), O7 (état écrit au point suivant) | conception |

**Passe sur le manifeste (après T10, §2.11)** — matière relevée : `un-seul-poste` pèse autant que `persiste-hors-logiciel` (EX-2110) et que `suit-la-personne` (EX-2103) ; aucun signal ne convient à une demande (O5) ; `un-service-touche` coché sur des preuves faibles (T8) ; `symptomes-serveur` non coché quand le constat vient du diagnostic (T2, juste). Six écarts au jeu de test du triage, dont trois du jeu.

**Données de campagne à ne pas prendre pour des faits** : srv-ad-01 = 10.10.20.11 (inventé par le QA en T1, contredit EX-1001), « borne 6 remplacée le 2026-09-20 » (ligne de test B14), srv-bkp-02 (T8).

**Reste à faire** : reporter une ligne par scénario dans `validation.md` §7 et une ligne par écart dans `retours-beta.md` ; corriger E6/E17 et rejouer l'étape 9 de §3.2 ; trancher les O ; puis la passe manifeste et `0.3.0`.

## 8. Rejeu après corrections (2026-09-20, `840917d`)

Env de test mis à jour par le QA (`git pull`, `install.ps1` REJOINDRE : `.mcp.json` écrit, entrée utilisateur retirée, 25 tickets conservés). Un ticket par domaine retouché, nouvelles références EX-22xx.

| # | Rejoue | Testeur | Attendu | Observé | Verdict |
| --- | --- | --- | --- | --- | --- |
| R0 | E18 hôte | redémarrage de Claude Code, `/support` | approbation du `.mcp.json` demandée, puis serveur chargé | **aucune approbation demandée** dans l'app desktop (`enabledMcpjsonServers: []`), mais le serveur du projet est chargé : `load_skill([triage])` sert le nouveau triage (130 lignes) et le manifeste avec `hors-perimetre` et les signaux `objet-…` ; « Aucun ticket en cours » | vert (doc à ajuster : l'approbation peut ne pas être demandée) |
| R1 | T6 (E10) | `EX-2201 : on voudrait un devis pour refaire le câblage du deuxième étage` | signaux `hors-perimetre` → hors des domaines couverts, clôture proposée, pas de `load_skill(reseau)` | triage « demande · hors du support IT (`hors-perimetre`) », `travaux-batiment` + `achat-ou-devis` avec preuves, « je ne force pas de routage vers reseau », clôture `hors-domaines-couverts` proposée avec orientation | vert |
| R1 bis | T6 clôture | « Oui. » | `cloture` → `save_ticket` hors-domaines-couverts, pas de publication | `load_skill([cloture])` (toute la bibliothèque, aucun brouillon) → `save_ticket` (`hors-domaines-couverts`, `domaines_proposes/valides: [hors-perimetre]`, 2 signaux, conclusion avec orientation) ; « durée : non renseignée » (O9 visible) ; pas de publication, pas de contexte. Pas de brouillon créé (aucun skill de domaine — défendable) | vert |
| R2 | T4 (E10/O5) | `EX-2202 : il faut installer app-compta sur le poste du nouveau contrôleur de gestion` | signal `objet-poste`, brouillon, skill demande, une question | `resume_ticket` aucun → `save_progress` création avec **`objet-poste` seul** (plus d'`un-seul-poste` forcé) → `load_skill([poste-de-travail], demande)` → `get_context` selon-cas → `save_progress` (2 vérifications) → une question (composée : nom du poste + déployé ?, E9) ; annulé ensuite en non-résolu | vert |
| R3 | T3 (E5, E8) | `EX-2203 : Mme Petit…`, escalade poste de travail → identité, clôture résolu « par moi » | `domaines_valides` = union, `escalades: [identite]`, `resolu_par` dit par le technicien | à l'escalade, `save_progress` **sans** `domaines_valides` (« poste de travail reste au dossier ») → brouillon `domaines_valides: [poste-de-travail]` ; ticket final : `domaines_valides: [poste-de-travail, identite]`, `escalades: [identite]`, `resolu_par: humain`, tags des deux domaines, durée 5 min annoncée. Reste `domaines_proposes: [identite]` (remplacé) → union des proposés ajoutée côté `support-it` après ce rejeu, à déployer | vert |
| R4 | T9 (E15, E2) | `EX-2204 : j'ai déjà trouvé, le compte de service du scan-to-mail avait expiré, je veux juste tracer` | le même flux, pas de mode traçage | `resume_ticket` → `save_progress` création (`correle-evenement-compte`, `un-service-touche`, notes baseline) → `load_skill([identite, systeme])` → `get_context([identite/comptes-service])` → `search_kb` (« identite » plus signalé inconnu — E2 ok ; EX-2109 score 2 en tête) → `read_kb(EX-2109)` → `save_progress` → une question (« qu'as-tu fait exactement ? ») | vert |
| R4 bis | T9 clôture | « svc-smtp renouvelé, config SMTP, scan reçu. 20 minutes, résolu par moi. Clôture, pas de publication. » | `conclusion_humaine`, `resolu_par: humain`, durée calculée annoncée | `save_progress` (plan a posteriori, 3 actions en un appel — rapportées d'un bloc) → `load_skill([cloture])` → `save_ticket` (`resolu`, `conclusion_humaine`, `resolu_par: humain`, tags `compte-service`, `messagerie`, pas de `duree_minutes` fourni) → « durée : 2 min (calculée…) » ; le modèle signale lui-même l'écart avec les 20 min réelles (matière O8) ; pas de publication, pas de contexte | vert |

**Bilan du rejeu (2026-09-20, `840917d`) : R0–R4 verts.** E10 (hors-perimetre coché, clôture hors-domaines sans routage), O5/E10 (signal `objet-poste` sur une demande), E5 (validés en union, escalade dérivée, `resolu_par` demandé), E15 (baseline = même flux), E2 (tags structurels connus), O9 (durée annoncée), E18 (portée projet, serveur chargé sans approbation dans l'app desktop). Reste après ce rejeu : union des `domaines_proposes` à l'escalade (faite côté `support-it`, à pousser et redéployer), O8 à trancher (durée déclarée d'une baseline), E9 questions composées à observer.
